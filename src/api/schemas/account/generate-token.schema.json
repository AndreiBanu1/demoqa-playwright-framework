{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "POST /Account/v1/GenerateToken — 200 response (successful authorization)",
  "description": "Failed authorization returns the same 200 status but with token/expires set to null and status 'Failed'; that variant is asserted explicitly in the spec rather than via this schema.",
  "type": "object",
  "required": ["token", "expires", "status", "result"],
  "additionalProperties": false,
  "properties": {
    "token": { "type": "string", "minLength": 1 },
    "expires": { "type": "string", "format": "date-time" },
    "status": { "type": "string", "enum": ["Success"] },
    "result": { "type": "string", "minLength": 1 }
  }
}
